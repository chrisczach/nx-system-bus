{
  "name": "nx-system-bus",
  "private": false,
  "version": "0.1.3",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "export MODE=library && tsc && vite build",
    "deploy": "export MODE=deploy && npm run build",
    "prepublish": "export MODE=library && tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/lodash-es": "^4.17.6",
    "typescript": "^4.6.4",
    "vite": "^3.0.0",
    "vite-plugin-mkcert": "^1.9.0"
  },
  "dependencies": {
    "lodash-es": "^4.17.21",
    "rxjs": "^7.5.6"
  }
}
